/**
 * Raporlama katmanı — Chart.js, zaman kazancı, Gemini raporu (Sekme 6).
 */
export function kpiStub() {
  return {
    openRequests: 0,
    criticalCount: 0,
    activeRoutes: 0,
    secondsSavedTotal: 0,
  };
}
